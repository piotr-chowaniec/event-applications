version: '3'

services:
  event-applications:
    build: .
    command: npm run dev
    volumes:
      - .:/app
      - /app/node_modules
      - /app/client/node_modules
    ports:
      - 3000:3000
